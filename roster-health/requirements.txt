# Core (needed to run the Sleeper-only pipeline)
pydantic>=2.6,<3
PyYAML>=6.0
requests>=2.31

# RSS + HTML scraping (needed by the official-tier adapters, deliverable #4)
feedparser>=6.0
beautifulsoup4>=4.12
selectolax>=0.3

# ESPN private-league roster pull (deliverable #3) + league-true projections
# (sources/espn_fantasy.py talks to the ESPN Fantasy API directly for scoring,
# and reuses these cookies; espn-api is still used for roster identity.)
espn-api>=0.44

# Dev / CI only — offline unit tests (tests/)
pytest>=8.0
